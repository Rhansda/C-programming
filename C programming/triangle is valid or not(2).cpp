#include<stdio.h>
int main()
{
	int a1,a2,a3,total;
	printf("Enter the value of a1: ");
	scanf("%d",&a1);
	printf("Enter the value of a2: ");
	scanf("%d",&a2);
	printf("Enter the value of a1: ");
	scanf("%d",&a3);
	if(total==180)
	{
		if(a1<a2+a3)
		{
			if(a2<a1+a3)
			{
				if(a3<a1+a2)
			}
		}
		printf("%d is a valid triangle",total);
		else
		{
			printf("%d is not a valid triangle",total);
		}
		return 0;
	}
}
