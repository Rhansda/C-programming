#include<stdio.h>
int main()
{
	int angle1,angle2,angle3, total=180;
	printf("Enter the value of angle1: ");
	scanf("%d",&angle1);
	printf("Enter the value of angle2: ");
	scanf("%d",&angle2);
	printf("Enter the value of angle3: ");
	scanf("%d",&angle3);
	if(total=angle1+angle2+angle3);
	{
		printf("%d is valid triangle",total);
	}
	else
	{
		printf("%d is not a valid triangle",total);
	}
	return 0;
}
